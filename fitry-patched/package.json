{
  "scripts": {
    "build": "cd fitry-app && npm install && npm run build",
    "deploy": "cd fitry-app && npm run build && npx firebase deploy --only hosting --project fitry-2df38"
  },
  "config": {
    "firebase_project": "fitry-2df38"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "firebase-admin": "^13.10.0",
    "firebase-tools": "^15.15.0"
  }
}
